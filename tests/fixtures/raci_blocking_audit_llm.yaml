mission:
  key: raci-blocking-bad
  name: RACI Blocking Audit with LLM Responsible
  version: "1.0.0"
steps:
  - id: step-01
    title: Initial step
    prompt: "Do something"
audit_steps:
  - id: audit-01
    title: Blocking audit with bad RACI
    depends_on: ["step-01"]
    audit:
      trigger_mode: manual
      enforcement: blocking
    raci:
      responsible:
        actor_type: llm
      accountable:
        actor_type: human
    raci_override_reason: "Testing blocking audit with LLM responsible"
