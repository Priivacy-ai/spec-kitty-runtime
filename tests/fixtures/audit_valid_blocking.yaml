mission:
  key: valid-blocking
  name: Valid Blocking Mission
  version: "1.0.0"
steps:
  - id: step-01
    title: Initial step
audit_steps:
  - id: audit-01
    title: Post-merge policy check
    depends_on: ["step-01"]
    audit:
      trigger_mode: post_merge
      enforcement: blocking
